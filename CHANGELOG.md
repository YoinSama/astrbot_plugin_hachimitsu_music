# 更新日志

## v1.3.0（2026-09-22）

### 新增

- **随机池：自己决定从哪些池子里抽**。可选「总榜（前 N 名）」+ 榜单里出现过的**全部风格标签**，
  可多选、至少留一个；概率按选中的池子数**均分**（选 5 个就是每个 20%）。
- **单个池子的概率可以手动指定**。在控制台页点池子右边的 **✎** 改成手动并填数值（点 **×** 改回自动），
  没手动指定的池子会自动均分剩下的概率 —— 例如 5 个池里把 A 固定成 50%，其余四个自动变 12.5%。
  - 单池最低 1%、总和不超 100%，填超了会自动压回当前可填的最大值并说明原因
  - 概率保留 1 位小数，前后端用同一套公式，界面上看到的就是实际抽歌用的值
- 控制台「常用配置」里新增随机池区块：概率条（深色块=手动、浅色块=自动）、池子规模、
  搜索框、「全选 / 恢复默认」按钮；一个池子都没选时保存按钮直接禁用

### 变更

- **移除 `random.top_weight` / `random.style_weight`**（旧的总榜权重 / 每风格权重），
  改为 `random.pools`（选中的池）+ `random.pool_weights`（手动指定的概率）。
  升级时插件会把旧权重**一次性换算**成等价的池子选择并写回配置，不会变成空池

### 稳定性

- 抽中的池子当前取不到歌（标签本期消失 / 名字打错）时，概率让给其它选中的池，
  不会再像以前那样一律回退总榜
- 所有选中的池都取不到歌时才回退整榜；任何情况都不会返回空

## v1.2.2（2026-09-20）

### 修复
- 补全 B站 不可见稿件（code=62012）的错误处理：在 `get_playurl`（获取音轨）处补上 `BiliError` 捕获，命中失效稿件码时 `mark_dead` 并转 `DurationRejected`，由上层自动换一首 / 提示「换一首试试」，不再冒泡成含糊的「点歌出错了」。
- `main.py` 的 `_deliver` 在大 `try` 的通用兜底前新增专门的 `except BiliError` 分支，给出带 code 的明确提示「B站 接口返回错误（code=xxx），这首暂时点不了，请稍后再试或换一首」。
- `DEAD_VIEW_CODES` 由 4 个扩至 6 个（新增 `-688` 地区限制、`-689` 版权限制），同属永远无法播放的稿件，一并自动跳过。

### 优化
- `core/utils.py` 的报错日志路径指引改为版本无关写法：主推 WebUI 实时日志 / 启动控制台，并注明 AstrBot v4 默认不写日志文件（`log_file_enable=false`），避免误导用户去找不存在的 `data/logs/astrbot.log`。

## v1.2.1（2026-09-16）

### 新增

- **配额列表看得懂了**：以前只有一串群号和 QQ 号，现在会显示**群名和 QQ 昵称**，
  名字下面一行小字才是号码
- **显示真实头像**：配额列表里直接显示**群头像和 QQ 头像**；网络不通取不到头像时，
  自动退回显示名字的第一个字，不会留空白
- **配额区加了「刷新」按钮**：点一下就能看到最新的点歌次数，不用刷新整个网页
- **「补全名称」按钮**：名字没显示出来时点一下，会重新去 QQ 那边拉一次群名和昵称

### 改进

- **所有按钮点了都有回音**：保存配置、重置配额、发起登录请求等等，成功或失败都会
  弹出提示，不会再出现「点了没反应、不知道成没成」的情况
  - 提示条会**跟着滚动停在页面顶部**，滚到哪儿都看得见
  - 操作进行中按钮会暂时点不动，避免连点把提示冲掉
- 配额的搜索框支持**按名字搜**了（以前只能输号码）；手动重置的框里也可以直接
  输入群名或昵称
- 群配额上限这类很长的数字，现在会**自动换行显示**，不再把名字挤变形
- 控制台左上角的图标换成了插件自己的 logo（以前是个临时的音符图标）

### 修复

- **「重置全部配额」和「清空音频缓存」点了完全没反应**：这两个按钮原来用的是
  浏览器的原生确认框，而网页控制台运行在受限的沙箱里，浏览器会直接忽略这个确认框
  并且当作「取消」处理，导致按钮彻底失效（网页控制台里会看到
  `Ignored call to 'confirm()'` 的报错）。现在改成了插件自己画的确认面板，
  正常弹、能取消、点了确定才会真的执行

## v1.2.0（2026-09-15）

> ⚠️ **「填 0」不再表示「关闭限制」了！**
> 以前有几项配置填 0 会被当成「不限制」。从这版开始，要关掉某项限制请统一填 **-1**；
> 填 0 会被当成无效值，自动改成 1（最小可用值），并在日志和网页控制台里提示你。

### 新增

- **想关掉哪项限制，就把那一项填 -1**。支持这一用法的有 7 项：用户冷却、群冷却、
  用户日配额、群日配额、全局每分钟上限、最大并发数、队列上限
- **作品长度限制**：太长或太短的作品会自动跳过，不会发给群里
  - 默认踢掉 10 分钟（600 秒）以上的作品，实测约 4.2%，最长的一首有 43 分钟
  - 随机点歌抽到太长的会**自动换一首**，你完全感觉不到（一次查询只要 0.08 秒）
  - 搜索结果里超时的作品直接不显示，用后面的候选顶上，仍然给你 5 条
  - 已经失效的稿件（约 1.7%）也会一并跳过，省掉必然失败的点歌
  - 判断时长**不额外发任何请求**，所以冷启动就准确，也不用等漫长的预热
- 填了无效值（比如填 0 或者乱填文字）时，插件会自动改成能用的值，并在
  启动日志和网页控制台里明确告诉你「哪一项被改成了什么」

### 改进

- 网页控制台新增「最短时长 / 最长时长 / 自动换几首」三个配置项，改完立即生效
- 控制台的状态页能看到当前的时长规则和已经记录了多少首歌的时长
- 控制台保存配置时，如果填的值被自动纠正了，会直接弹出提示告诉你实际生效的值
- 队列上限填 -1 后，状态页显示「不限制」而不是 -1

### 修复

- 最大并发数填 0 会让插件**彻底卡死且没有任何报错**（最严重的一类问题），现在会自动改成 1
- 老版本升级上来、配置文件里没有这些新配置项时，会沿用默认值，不会突然把限流收紧

## v1.1.1（2026-09-15）

## 其他
- 补充readme说明
- 上传正式插件logo

## v1.1.0（2026-09-14）

### 改进

- 扫码登录成功后，Cookie 会自动保存进插件配置；以后想退出登录，把配置里的 Cookie 删掉就行
- 管理员和机器人私聊过一次之后，插件会记住这个会话，重启插件也不用重新私聊
- 网页控制台的登录只保留扫码方式，去掉了「在浏览器打开登录页」按钮

### 修复

- 点「向管理员发起登录请求」时报错的问题
- 管理员明明发过私聊，机器人却说「请先私聊我」的问题
- 已经登录过的情况下，二维码一弹出来就提示「扫码成功」的误会
- 扫码登录成功后，网页控制台的登录状态一直不更新的问题（现在会自动刷新）

### 其他

- 更正插件仓库地址为新名字

## v1.0.0（2026-09-14）

### 首个版本

- `/哈基米` 随机点一首歌；`/哈基米 关键词` 搜索后回复序号点播；另有 `帮助` 和 `状态` 指令
- 歌曲来自 hajihami 的 B站 播放量总榜，随机时会照顾几个固定风格
- 歌曲自动下载、转码，以语音消息直接发到群里
- 可以扫码登录 B站 账号，解锁更高音质
- 自带防刷屏、每日点歌上限、黑名单等管理功能，并提供网页控制台
- Cookie 失效时会私聊管理员，回复「确定」即可扫码续期
